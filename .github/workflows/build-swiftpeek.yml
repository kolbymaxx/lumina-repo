name: Build SwiftPeek

# Build-only: upload rootless + roothide artifacts. Do NOT publish to APT / Pages.
on:
  workflow_dispatch:
  push:
    paths:
      - 'SwiftPeek/**'
      - '.github/workflows/build-swiftpeek.yml'
  pull_request:
    paths:
      - 'SwiftPeek/**'
      - '.github/workflows/build-swiftpeek.yml'

concurrency:
  group: swiftpeek-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build rootless .deb
    runs-on: macos-14
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build deps
        run: |
          brew install ldid dpkg make
          echo "$(brew --prefix make)/libexec/gnubin" >> "$GITHUB_PATH"

      - name: Setup Theos
        uses: waruhachi/theos-action@v2.4.6
        with:
          theos-src: 'theos/theos'
          theos-branch: 'master'
          sdks-src: 'theos/sdks'
          sdks-branch: 'master'
          libgcuniversal: 'false'
          altlist: 'false'

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcodebuild -version
          xcrun -sdk iphoneos --show-sdk-version

      - name: Build SwiftPeek (rootless)
        working-directory: SwiftPeek
        env:
          THEOS: ${{ env.THEOS }}
        run: |
          set -euo pipefail
          SDK_VER="$(ls "$THEOS/sdks" | sed -n 's/iPhoneOS\([0-9.]*\)\.sdk/\1/p' | sort -V | tail -1 || true)"
          if [ -n "${SDK_VER:-}" ]; then
            sed -i '' "s/^TARGET :=.*/TARGET := iphone:clang:${SDK_VER}:15.0/" Makefile
            echo "Using SDK $SDK_VER"
          fi
          sed -i '' 's/^Architecture: .*/Architecture: iphoneos-arm64/' control
          make clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless
          mkdir -p "$GITHUB_WORKSPACE/out"
          find . .. -name '*.deb' -path '*/packages/*' -print -exec cp {} "$GITHUB_WORKSPACE/out/" \;
          ls -lah "$GITHUB_WORKSPACE/out"
          DEB="$(ls "$GITHUB_WORKSPACE/out"/*.deb | head -1)"
          TMP=$(mktemp -d)
          dpkg-deb -x "$DEB" "$TMP"
          FILTER="$(find "$TMP" -name 'SwiftPeek.plist' -path '*DynamicLibraries*' | head -1)"
          PREFS_SRC="$GITHUB_WORKSPACE/SwiftPeek/layout/Library/PreferenceLoader/Preferences/SwiftPeek.plist"
          test -n "$FILTER"
          plutil -p "$FILTER"
          grep -q 'com.apple.Music' <(plutil -p "$FILTER")
          # 0.4.0 allows SpringBoard in the filter for the opt-in recon mode.
          # 0.2.0 put SpringBoard into Safe Mode with a Swift-linked dylib, so
          # what has to be guarded now is not the filter but the two things
          # that make the SpringBoard path safe: the dylib stays ObjC-only, and
          # injection is gated behind a pref that defaults off.
          if grep -qi 'springboard' <(plutil -p "$FILTER"); then
            echo "SpringBoard present in filter — verifying the opt-in gate"
            grep -q 'targetSpringBoard' "$GITHUB_WORKSPACE/SwiftPeek/src/SPAttach.m" || {
              echo "ERROR: SpringBoard in filter with no targetSpringBoard gate" >&2
              exit 1
            }
            grep -q 'targetSpringBoard' <(plutil -p "$PREFS_SRC") || {
              echo "ERROR: targetSpringBoard is not exposed in the prefs bundle" >&2
              exit 1
            }
            # An ObjC-only dylib is load-bearing here: Swift runtime + SpringBoard
            # is the exact 0.2.0 failure. No Swift sources may enter the build.
            if ls "$GITHUB_WORKSPACE"/SwiftPeek/src/*.swift >/dev/null 2>&1; then
              if grep -q '\.swift' "$GITHUB_WORKSPACE/SwiftPeek/Makefile"; then
                echo "ERROR: Swift source in a SpringBoard-injecting SwiftPeek" >&2
                exit 1
              fi
            fi
          fi
          PREFS="$(find "$TMP" -path '*/PreferenceLoader/Preferences/SwiftPeek.plist' | head -1)"
          test -n "$PREFS"
          plutil -p "$PREFS"
          grep -q 'dumpFieldMeta' <(plutil -p "$PREFS")
          dpkg-deb -I "$DEB"
          EXPECT_VER="$(sed -n 's/^Version: //p' control | head -1)"
          test -n "$EXPECT_VER"
          dpkg-deb -I "$DEB" | grep -q "Version: ${EXPECT_VER}"

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: SwiftPeek-rootless-deb
          path: out/*.deb
          if-no-files-found: error

  build-roothide:
    name: Build roothide .deb
    runs-on: macos-14
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build deps
        run: |
          brew install ldid dpkg make
          echo "$(brew --prefix make)/libexec/gnubin" >> "$GITHUB_PATH"

      - name: Setup Theos (roothide fork)
        uses: waruhachi/theos-action@v2.4.6
        with:
          theos-src: 'roothide/theos'
          theos-branch: 'master'
          sdks-src: 'theos/sdks'
          sdks-branch: 'master'
          libgcuniversal: 'false'
          altlist: 'false'

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcodebuild -version
          xcrun -sdk iphoneos --show-sdk-version

      - name: Build SwiftPeek (roothide)
        working-directory: SwiftPeek
        env:
          THEOS: ${{ env.THEOS }}
        run: |
          set -euo pipefail
          SDK_VER="$(ls "$THEOS/sdks" | sed -n 's/iPhoneOS\([0-9.]*\)\.sdk/\1/p' | sort -V | tail -1 || true)"
          if [ -n "${SDK_VER:-}" ]; then
            sed -i '' "s/^TARGET :=.*/TARGET := iphone:clang:${SDK_VER}:15.0/" Makefile
            echo "Using SDK $SDK_VER"
          fi
          sed -i '' 's/^Architecture: .*/Architecture: iphoneos-arm64e/' control
          make clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=roothide
          mkdir -p "$GITHUB_WORKSPACE/out"
          find . .. -name '*.deb' -path '*/packages/*' -print -exec cp {} "$GITHUB_WORKSPACE/out/" \;
          ls -lah "$GITHUB_WORKSPACE/out"
          DEB="$(ls "$GITHUB_WORKSPACE/out"/*.deb | head -1)"
          dpkg-deb -I "$DEB"
          EXPECT_VER="$(sed -n 's/^Version: //p' control | head -1)"
          test -n "$EXPECT_VER"
          dpkg-deb -I "$DEB" | grep -q "Version: ${EXPECT_VER}"
          if dpkg-deb -c "$DEB" | grep -q ' var/jb/'; then
            echo "ERROR: roothide deb still contains /var/jb paths" >&2
            exit 1
          fi

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: SwiftPeek-roothide-deb
          path: out/*.deb
          if-no-files-found: error
